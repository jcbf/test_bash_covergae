language: ruby
rvm:
   - 2.2.5
matrix:
env:
   - COVERAGE=1 DEBUG=1 
#   - COVERAGE=1
#   - COVERAGE=0

before_install:
  - sudo apt-get update -qq
install:
  - which gem
  - sudo /home/travis/.rvm/rubies/ruby-2.2.5/bin/gem install  coveralls
  - sudo /home/travis/.rvm/rubies/ruby-2.2.5/bin/gem install  bashcov
  - bundle install
  - set
  - misc/ci-setup.sh
#  - export PATH="$PATH:$PWD"
script:
  - misc/ci-runtest.sh
